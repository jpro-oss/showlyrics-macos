node_modules/
dist/
build/
bin/
backend/__pycache__/
backend/dist/
sender/
sender-1.1.1/
tools/
__pycache__/
.env
*.log

# Binary files (generated during CI build — too large for Git)
backend/playback-engine
backend/playback-engine-amd64
backend/playback-engine-arm64
backend/camera-service
backend/camera-service-amd64
backend/camera-service-arm64
backend/ffmpeg
backend/playback-engine.exe
playback-engine.exe
*.dll

# PyArmor runtime — HARUS di-commit! Dibutuhkan saat PyInstaller build.
# .so file (darwin_x86_64 & darwin_arm64) wajib ada di repo agar
# GitHub Actions dapat menyertakannya ke dalam binary.
# Tanpa ini, build GAGAL: ModuleNotFoundError: pyarmor_runtime_000000.darwin_*
# backend/pyarmor_runtime_000000/   ← JANGAN uncomment baris ini!

# PyInstaller
backend/build/

# Crash reports
backend/nuitka-crash-report.xml

# macOS system files
.DS_Store
._*

# Secrets
.session_token
*.pem
*.p12
*.key

# Go binaries
playback-engine/playback-engine